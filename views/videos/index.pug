extends ../layout

block content
    .container
        h1.header Videos (#{videos.length})
        if subtitle
            p.subtitle #{subtitle}

    
        .segment#channelSelectContainer

            .field 
                label.label(for="channelSelect") Channel Switcher
                .control
                    select.input#channelSelect

        #sortContainer
            .field 
                label.label(for="channelSelect") Sort by
                .control
                    select.input.capitalized#sortSelect
                        each sort, i in Object.keys(sorts)
                            option.capitalized(value=sort,selected=(currentSort == sort))=sort

            .field 
                label.label(for="channelSelect") Order
                .control
                    select.input#orderSelect
                        option(value=1) Acending
                        option(value=-1) Decending

            .field
                .buttons.equal
                    button.button.fluid#clearButton
                        span.icon
                            i.fas.fa-trash
                        span Clear Filter
                    button.button.fluid#searchButton
                        span.icon
                            i.fas.fa-search
                        span Search

            .divider

        .columns.multiline#videoGridContainer

block scripts
    script(src="/js/channelSwitcher.js") 
    script(src="/js/videos.js") 